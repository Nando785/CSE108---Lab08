# To create a virtual environment and install dependencies, run the following commands:
#
# 1) python3 -m venv venv               <- create environment 
#                                           (do so in root folder)
# 2) .\venv\bin\activate            <- activate environment 
#                                           (May need to adjust file directory in command)
# 3) pip install -r requriements.txt    <- install dependencies
#
# 4) deactivate                         <- exit environment

flask
flask-Login
flask-CORS
